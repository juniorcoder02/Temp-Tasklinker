<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link
  href="https://fonts.googleapis.com/css2?family=Karla:wght@400;700&family=Poppins:wght@600&display=swap"
  rel="stylesheet"
/>
  <title>Thank You!</title>
  <link rel="icon" href="/assests/link.png" type="image/png">

  <style>
    body {
      background: linear-gradient(to bottom, #000, #f39c12);
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      flex-direction: column;
      font-family: 'Poppins', sans-serif;
    }
    
    .thank-you-message {
      text-align: center;
      animation: fadeIn 1.5s ease-in-out;
    }
    
    .thank-you-message h1 {
      font-size: 3rem;
      margin-bottom: 20px;
    }
    
    .thank-you-message p {
      font-size: 1.2rem;
      margin-bottom: 40px;
    }

    .checkmark {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      background: #f39c12;
      position: relative;
      animation: popIn 0.5s ease-out;
    }

    .checkmark::after {
      content: '';
      width: 50px;
      height: 50px;
      border-left: 5px solid #fff;
      border-bottom: 5px solid #fff;
      position: absolute;
      left: 25%;
      top: 25%;
      transform: rotate(-45deg);
    }

    @keyframes popIn {
      0% {
        transform: scale(0);
        opacity: 0;
      }
      100% {
        transform: scale(1);
        opacity: 1;
      }
    }

    .home-button {
      background-color: #fff;
      color: #f39c12;
      padding: 10px 20px;
      border-radius: 5px;
      text-decoration: none;
      font-size: 1rem;
      transition: background-color 0.3s ease, transform 0.3s ease;
    }

    .home-button:hover {
      background-color: #e67e22;
      transform: scale(1.05);
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }
  </style>
</head>
<body>
  <div class="thank-you-message">
    <div class="checkmark"></div>
    <h1>Thank You <%= name %></h1>
    <p>Your message has been sent successfully. We will get back to you soon.</p>
    <a href="/landing-page" class="home-button">Go to Home</a>
  </div>
</body>
</html>
